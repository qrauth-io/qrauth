# Python SDK (Discontinued)

The Python SDK (`qrauth` on PyPI) has been discontinued as of 2026-04-16.

Use the [Node.js SDK](/sdk/node) for server-side integration, or the [Web Components](/sdk/web-components) for browser-based flows.
