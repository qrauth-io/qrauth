{
  "name": "qrauth",
  "version": "0.1.0",
  "private": true,
  "description": "QRAuth Platform - Open source QR authentication platform",
  "author": "QRAuth",
  "license": "BSL-1.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.3.3",
    "eslint": "^8.55.0",
    "prettier": "^3.1.0"
  }
}
